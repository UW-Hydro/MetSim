name: test_env
channels:
  - conda-forge
dependencies:
  - python=3.5
  - xarray
  - pandas
  - netcdf4
  - numba
  - bottleneck
  - numpy
  - scipy
  - pytest
  - pip:
    - coveralls
    - pytest-cov
