name: test_env
channels:
  - conda-forge
dependencies:
  - python=3.6
  - xarray=0.10.9
  - pandas
  - netcdf4
  - bottleneck
  - numba
  - numpy
  - scipy
  - pytest
  - pip:
    - coveralls
    - pytest-cov
